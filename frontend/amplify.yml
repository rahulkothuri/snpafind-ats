# ============================================================================
# AWS Amplify Build Configuration for SnpaFind ATS Frontend
# ============================================================================
# This file configures the build process for AWS Amplify Hosting
# ============================================================================

version: 1

frontend:
  phases:
    # Pre-build phase: Install dependencies
    preBuild:
      commands:
        - echo "Installing dependencies..."
        - npm ci
    
    # Build phase: Build the Vite React application
    build:
      commands:
        - echo "Building the application..."
        - echo "VITE_API_URL=$VITE_API_URL" >> .env.production
        - npm run build
  
  # Artifacts configuration
  artifacts:
    # Vite outputs to 'dist' directory by default
    baseDirectory: dist
    files:
      - '**/*'
  
  # Cache configuration for faster builds
  cache:
    paths:
      - node_modules/**/*

# Custom headers for security and caching
customHeaders:
  - pattern: '**/*'
    headers:
      - key: 'Strict-Transport-Security'
        value: 'max-age=31536000; includeSubDomains'
      - key: 'X-Content-Type-Options'
        value: 'nosniff'
      - key: 'X-Frame-Options'
        value: 'DENY'
      - key: 'X-XSS-Protection'
        value: '1; mode=block'
  
  # Cache static assets for 1 year
  - pattern: 'assets/**/*'
    headers:
      - key: 'Cache-Control'
        value: 'public, max-age=31536000, immutable'
  
  # Don't cache HTML files
  - pattern: '*.html'
    headers:
      - key: 'Cache-Control'
        value: 'no-cache, no-store, must-revalidate'

# NOTE: Redirects must be configured in Amplify Console under
# "Hosting" > "Rewrites and redirects", not in this file.
# Add this rule in the console:
#   Source: </^[^.]+$|\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|woff2|ttf|map|json|webp)$)([^.]+$)/>
#   Target: /index.html
#   Type: 200 (Rewrite)
